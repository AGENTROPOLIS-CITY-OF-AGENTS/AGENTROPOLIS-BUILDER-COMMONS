{
  "name": "agentropolis-builder-commons",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Persistent collaborative build world for humans and agents.",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "validate": "node scripts/validate.mjs",
    "check": "npm run validate && npm test"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "Apache-2.0"
}
